# API Credentials - NEVER COMMIT!
config.json
.env

# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python

# Logs
memory/
*.log
*.jsonl

# IDEs
.vscode/
.idea/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# One-time scripts (bevatten embedded credentials)
do-upgrade.sh
upgrade-to-hybrid.sh
deploy-fixes.sh
setup-tailscale.sh
setup-tailscale-fixed.sh
sync-docs.sh

# Interne docs (bevatten wachtwoorden/IPs)
UPGRADE-INSTRUCTIES.md
SETUP-INSTRUCTIES.md
UPGRADE-SAMENVATTING.md

# Applied patches
*.patch

# Backtest data (regenereerbaar via data_collector.py)
backtest/data/*.csv
# OKX-sourced backfill (regenereerbaar via backtest/okx_backfill.py)
backtest/data/okx/
backtest/results/regime_diagnostics.csv
backtest/results/sweep/
backtest/results/archive/

# Plan E runner operational state (portfolio, trade log, runner cache)
state/

# Archive (oude configs met credentials)
archive/

# Trained model artifacts (data-derived, reproducible from code)
scripts/models/
*.joblib

# Edge-diagnosis: large regenerable per-bar export (regen via backtest/diag_export.py)
backtest/results/diag_bars.csv
# OKX-sourced backfill is already ignored above; same for Hyperliquid (regen via backtest/hyperliquid_backfill.py)
backtest/data/hyperliquid/

# graphify knowledge-graph outputs (regenereerbaar via `graphify .` — grote HTML/JSON)
graphify-out/

# Lokaal-only architectuur-audit (meta over de repo, niet publiceren)
docs/ARCHITECTURE-AUDIT-2026-06-07.md
